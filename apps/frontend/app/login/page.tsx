import LoginForm from "@/features/auth/components/login-form";

const LoginPage = () => {
  return (
    <section className="w-full h-screen flex justify-center items-center bg-sf-bg">
      <LoginForm />
    </section>
  );
};

export default LoginPage;
