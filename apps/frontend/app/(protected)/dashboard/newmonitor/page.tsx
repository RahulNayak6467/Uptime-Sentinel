import NewMonitorProperties from "@/features/new_monitor/components/new-monitor";

const NewMonitor = () => {
  return (
    <section className="pb-8">
      <NewMonitorProperties />
    </section>
  );
};

export default NewMonitor;
